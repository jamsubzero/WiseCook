<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mit.jamsubzero.wisecook"
    >

     <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainFragment"
            android:icon="@drawable/ic_launcher"
            android:label="WiseCook"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustPan"  >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RecipeResult.RecipeMainActivity"
            android:label="@string/title_activity_display_recipe_results"
            android:icon="@drawable/ic_launcher"
            android:theme="@style/AppTheme"/>

        <activity android:name=".RecipeResult.DisplayRecipe"
            android:label="@string/title_activity_display_recipe"
            android:icon="@drawable/ic_launcher"
            android:theme="@style/AppTheme"/><!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>